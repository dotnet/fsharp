<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework Condition="'$(OS)' == 'Unix'">netcoreapp2.0</TargetFramework>
    <TargetFrameworks Condition="'$(OS)' != 'Unix'">netcoreapp2.0;net45</TargetFrameworks>
    <DefineConstants>INTERNALIZED_FSLEXYACC_RUNTIME;$(DefineConstant)</DefineConstants>
  </PropertyGroup>

  <ItemGroup>
    <Compile Include="fslex.fsx" />
  </ItemGroup>

</Project>
