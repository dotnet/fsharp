
module M


    // Check we get parsing error and decent recovery
    let negParsingTest2() = {| b = 2 }

    let normalCode1() = ()
    // Check we get parsing error and decent recovery
    type X = {| A : int }

    let normalCode2() = ()

