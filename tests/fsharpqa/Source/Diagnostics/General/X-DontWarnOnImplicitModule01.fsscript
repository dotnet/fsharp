// #Regression #Diagnostics 
// Regression test for FSHARP1.0:2893
// The filename here is integral part of the test (the dash would trigger the warning if we has a .fs extension)
// We want to verify that when the extension is .fsx
// we do not emit any warning like:
// "The declarations in this File will be placed in an implicit module..."
#light
exit 0
