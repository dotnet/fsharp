# We should have tests for all the other constructs that can be put in an explicit object constructor!
	SOURCE=new_while_01.fs    COMPILE_ONLY=1	# new_while_01.fs
	SOURCE=WithAttribute01.fs COMPILE_ONLY=1 SCFLAGS="-a --test:ErrorRanges"	# WithAttribute01.fs
	SOURCE=WithAttribute02.fs COMPILE_ONLY=1 SCFLAGS="-a --test:ErrorRanges"	# WithAttribute02.fs