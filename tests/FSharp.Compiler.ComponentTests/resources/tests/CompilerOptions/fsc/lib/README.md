# Lib Tests Resource Directory

This directory is a placeholder for source files from the migrated lib tests.

The tests in `CompilerOptions/fsc/lib/lib.fs` use inline source strings since
the original tests primarily validated compiler option syntax, which doesn't
require external source files.

Original tests migrated from: `tests/fsharpqa/Source/CompilerOptions/fsc/lib`
